<UserControl x:Class="NotoNote.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NotoNote.Views" xmlns:viewmodels="clr-namespace:NotoNote.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}"
             mc:Ignorable="d"
             Width="600"
             MinHeight="600"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8,8,8,0" MaxWidth="600">
        <TabControl>
            <TabItem>
                <TabItem.Header>
                    <TextBlock Text="General"/>
                </TabItem.Header>
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <!-- API KEY -->
                        <TextBlock Text="API Key: OpenAI" FontWeight="Bold" TextWrapping="WrapWithOverflow"/>
                        <TextBox Margin="0,8,0,0" Text="{Binding OpenAiApiKey}"/>

                        <!-- HOTKEY 1-->
                        <TextBlock Text="Hotkey: Activation" FontWeight="Bold" Margin="0,8,0,0"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                                <Style TargetType="CheckBox">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="Shift: "/>
                            <CheckBox IsChecked="{Binding HotkeyActivation.Shift}"/>
                            <TextBlock Text="Ctrl: " Margin="4,0,0,0"/>
                            <CheckBox IsChecked="{Binding HotkeyActivation.Ctrl}"/>
                            <TextBlock Text="Alt: " Margin="4,0,0,0" />
                            <CheckBox IsChecked="{Binding HotkeyActivation.Alt}"/>
                            <TextBlock Text="Key: " Margin="4,0,0,0" />
                            <ComboBox ItemsSource="{Binding AvailableKeys}" Height="20" />
                        </StackPanel>

                        <!-- HOTKEY 2 -->
                        <TextBlock Text="Hotkey: Toggle profiles" FontWeight="Bold" Margin="0,8,0,0"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4" Height="30">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                                <Style TargetType="CheckBox">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="Shift: "/>
                            <CheckBox IsChecked="{Binding HotkeyToggleProfile.Shift}"/>
                            <TextBlock Text="Ctrl: " Margin="4,0,0,0"/>
                            <CheckBox IsChecked="{Binding HotkeyToggleProfile.Ctrl}"/>
                            <TextBlock Text="Alt: " Margin="4,0,0,0" />
                            <CheckBox IsChecked="{Binding HotkeyToggleProfile.Alt}"/>
                            <TextBlock Text="Key: " Margin="4,0,0,0" />
                            <ComboBox ItemsSource="{Binding AvailableKeys}" Height="20" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <TextBlock Text="Profiles"/>
                </TabItem.Header>
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Profiles list -->
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="22"/>
                        </Grid.RowDefinitions>
                        <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" SelectionMode="Single" Grid.Row="0" Margin="1" DisplayMemberPath="Name.Value">
                        </ListBox>

                        <Grid Grid.Row="1" Height="20">
                            <Grid.Resources>
                                <Style TargetType="Button">
                                    <Setter Property="Margin" Value="1"/>
                                    <Setter Property="FontSize" Value="10"/>
                                </Style>
                            </Grid.Resources>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="↑" Command="{Binding MoveProfileToUpCommand}" VerticalContentAlignment="Stretch"/>
                            <Button Grid.Column="1" Content="↓" Command="{Binding MoveProfileToDownCommand}"/>
                            <Separator Grid.Column="2" Background="Transparent"/>
                            <Button Grid.Column="3" Content="＋" Command="{Binding AddProfileCommand}"/>
                            <Button Grid.Column="4" Content="ー" Command="{Binding DeleteProfileCommand}"/>
                        </Grid>
                    </Grid>

                    <!-- Edit -->
                    <ScrollViewer Grid.Column="1" 
                                  HorizontalScrollBarVisibility="Disabled" 
                                  VerticalScrollBarVisibility="Auto"
                                  Margin="8,0">
                        <Grid>
                            <Grid.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="2,4"/>
                                </Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Margin" Value="2"/>
                                </Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="Margin" Value="2"/>
                                </Style>
                            </Grid.Resources>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="24"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Edit" FontWeight="Bold"/>
                            <TextBlock Grid.Row="1" Text="Name"/>
                            <TextBox Grid.Row="2" Text="{Binding SelectedProfileName, UpdateSourceTrigger=LostFocus, Mode=TwoWay}"/>
                            <TextBlock Grid.Row="3" Text="System Prompt"/>
                            <TextBox Grid.Row="4" MinHeight="64" MaxHeight="500"
                                     Text="{Binding SelectedProfileSystemPrompt}"
                                     TextWrapping="Wrap" AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" />
                            <TextBlock Grid.Row="5" Text="Transcription Model"/>
                            <ComboBox Grid.Row="6" ItemsSource="{Binding AvailableTranscriptionAiModels}" SelectedItem="{Binding SelectedTranscriptionAiModelId}" DisplayMemberPath="Value"/>
                            <TextBlock Grid.Row="7" Text="Chat Model"/>
                            <ComboBox Grid.Row="8" ItemsSource="{Binding AvailableChatAiModels}" SelectedItem="{Binding SelectedChatAiModelId}" DisplayMemberPath="Value"/>
                        </Grid>
                    </ScrollViewer>

                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
